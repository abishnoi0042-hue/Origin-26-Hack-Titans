# Frontend Environment Variables (Vite)
# Only needed if you deploy backend to an external cloud server (Render, Railway, etc.)
# For local development or standalone Vercel/Netlify deployment, leave blank:
VITE_API_BASE_URL=
